const API_ROOT = "http://localhost:9000/api";

export const APIUrls = {
  addword: () => `${API_ROOT}/addword`,
  // signup: () => `${API_ROOT}/users/signup`,
  // fetchPosts: (page = 1, limit = 5) =>
  //   `${API_ROOT}/posts?page=${page}&limit=${limit}`,
};
